spring:
  application:
    name: TM66

upload:
  comment-dir: upload
  worker-dir: worker

user:
  kgs:
    email: ${KGS_EMAIL}
    ftp-path: ${KGS_FTP}

app:
  ftp:
    host: ${HOST}
    port: ${PORT}
    user: ${USER}
    password: ${PSW}